# Windows 공개 베타 사용 안내

connector.20은 Windows x64용 미서명 설치형 및 포터블입니다. [공식 다운로드](https://overcharge.io/download/)의 버전 고정 링크를 사용해 주세요. Windows가 미확인 게시자 경고를 표시하거나 실행을 차단할 수 있습니다. 보안 기능을 끄지 마세요.

1. 설치형은 현재 사용자에게 설치하고 시작 메뉴 바로가기를 만듭니다. 관리자 권한을 요청하지 않습니다. 포터블은 EXE를 직접 실행합니다.
2. Connection에서 Install / repair connection을 선택합니다. Dota가 감지되지 않으면 설치 폴더를 선택합니다.
3. 준비되면 Launch Dota 2를 직접 누릅니다. 이미 실행 중인 게임은 종료하거나 재시작하지 않습니다. 연결 파일을 고친 경우 편한 시점에 직접 게임을 재시작해 주세요.
4. Listening은 수신 준비이며 GSI receiving은 데이터 도착입니다. 데이터에 내 영웅만 포함될 수 있습니다. 다른 플레이어의 픽은 자동 화면 인식으로 보완합니다.
5. 매치 화면에서 Show tabs 또는 Esc로 탐색을 복원합니다. Heroes, Matches, Meta, Pros는 앱 내부 웹 탭입니다. Play와 연결 설정은 로컬에서 동작합니다.
6. 업데이트는 수동입니다. Agent를 닫고 새 설치형을 실행하거나 포터블 EXE를 교체합니다. 설치 설정은 사용자 기록을 보존하도록 구성되어 있으나 전체 설치·업데이트·제거 과정의 실제 검증은 미완료입니다.

## 자동 화면 인식

Dota 프로세스가 감지되면 Agent 내부에서 자동 시작합니다. 별도 인식 창을 열 필요가 없습니다. 대상은 Dota 창이며 오디오·외부 업로드·프레임 파일 저장은 하지 않습니다. 네이티브 캡처 스트림은 Dota 창을 받아오고, 분석용 캔버스에서는 상단 1440×128 영역만 처리합니다.

서로 다른 세 프레임에서 안정적으로 일치한 영웅을 Screen estimate로 반영하고 불완전한 GSI 갱신 때문에 지우지 않습니다. 세션이 바뀌면 초기화하며, 서로 다른 열 영웅 인식이 끝나면 캡처를 중지합니다. GSI와 수동 계획이 우선합니다. 알 수 없는 픽이나 밴을 만들어 내지 않습니다. 해상도·스킨·가려진 화면에 따라 오인식 또는 누락이 있을 수 있습니다.

Drow/Lich에는 공식 초상화와 수정된 픽 화면 크롭을 사용합니다. 저장된 화면의 오프라인 재생에서 전체 열 슬롯과 최적화 전후 결과 일치를 확인했습니다. 최종 버전의 실게임 전체 픽/밴, 모든 해상도·스킨은 검증되지 않았습니다.

## Matches와 Steam 로그인

Matches의 Sign in through Steam을 선택하면 같은 격리된 웹 탭 안에서 Steam 인증 화면을 엽니다. connector.20은 기존 탐색 제한이 인증 경로를 막던 문제를 수정합니다. Steam이 표시하는 인증 절차를 직접 완료한 뒤 Matches로 돌아옵니다. 서버의 로그인 검증과 브라우저 세션 보호는 유지됩니다. 실제 계정 로그인 완료와 로그인 후 경기 기록 조회는 아직 검증하지 못했습니다. 로컬 GSI 및 화면 인식에는 웹 로그인이 필요하지 않습니다.

## 포트 충돌과 진단

Retry receiver는 점유된 포트를 피하고 성공한 로컬 포트를 저장합니다. 이후 Install / repair connection으로 게임 연결 파일을 갱신합니다. 다른 프로그램을 종료하거나 방화벽 설정을 변경하지 않습니다. 선택형 GSI 로컬 기록은 화면 인식과 별개이며 기본적으로 꺼져 있습니다. 개인 캡처와 연결 토큰을 공유하지 마세요.

## 검증 및 향후 서명

최종 EXE 크기·SHA-256·소스 커밋·Io 해시는 release.json과 SHA256SUMS에 기록합니다. 체크섬은 게시자 서명을 대신하지 않습니다. 알려진 숨김 UI 검사 실패와 미검증 항목은 RELEASE_NOTES.md를 확인해 주세요.

향후 서명은 발급 자격 확인 → 인증서 또는 서명 서비스 준비 → 앱·설치기 서명 및 타임스탬프 → 서명 검증 → 새 버전·체크섬 게시 순서입니다. 이번 배포에는 가입·결제가 없습니다. 서명 후에도 SmartScreen 경고가 즉시 사라진다고 약속하지 않습니다. 상세 절차는 SIGNING.md에 있습니다.
